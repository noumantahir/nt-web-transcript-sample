import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
      flex: 1,
      maxWidth: 700,
    },
    listContent: {
      flex: 1,
      padding: 16,
      maxWidth: 700,
    }
  });