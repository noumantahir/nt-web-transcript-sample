import { StyleSheet } from "react-native";

export default StyleSheet.create({
    titleText: {
        fontSize: 18,
        fontWeight: 'bold',
    },
    bodyText: {
        fontSize: 16,
        marginBottom: 4,
    },
    labelText: {
        fontWeight: 'bold',
        fontSize: 14,
        marginBottom: 4,
    },
    captionText: {
        fontSize: 12,
        color: '#666',
    },
});
